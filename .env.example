# Jira credentials (optional — can also be entered in the UI)
JIRA_EMAIL=you@company.com
JIRA_API_TOKEN=your-api-token-here

# Optional: default Jira domain (e.g. mycompany.atlassian.net)
# If set, the app will pre-fill this. Otherwise it's parsed from the URL.
JIRA_DOMAIN=
